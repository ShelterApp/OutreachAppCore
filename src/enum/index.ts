export enum UserRole {
    Admin = 'Admin',
    OrgLead = 'OrgLead',
    Volunteer = 'Volunteer',
}

export enum UserStatus {
    Enabled = 1,
    Disabled = 3
}

export enum UserVerify {
    Verified = 1,
    Unverified = 0,
}

export enum OrganizationStatus {
    Enabled = 1,
    Disabled = 3
}